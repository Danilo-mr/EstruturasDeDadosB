
/*
bool estaOrdenado(int v[]) {
    int i;
    for(i=0; i<n-1; i++)
        if(v[i] > v[i+1])
            return false;
    return true;
}

for(stop=0; stop<n-1 && flag==0; stop++){
  for(i=n-1; i>stop && flag==0; i--){
        if(estaOrdenado(v)==true)
        {
            cout << "\n\nJa esta ordenado\n\n";
            flag=1;
        }
        qtd_comp++;
        if(v[i]<v[i-1]){ 
            qtd_trocas++;
            aux=v[i];
            v[i]=v[i-1];
            v[i-1]=aux;
        }
    }
  }*/
 